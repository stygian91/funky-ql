import language from "../../src/language";
import query from "../../src/transpilers/query";

describe('Transpiles query', () => {
  test('filter', () => {
    // const ast = language.Query.tryParse('where `a` > `qwe`')
    // const res = query(ast);
  });
});
